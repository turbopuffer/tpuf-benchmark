{
    "upserts": [
        {{ .UpsertBatchPlaceholder }}
    ],
    "distance_metric": "cosine_distance",
    "schema": {
        "vector": {
            "ann": true,
            "type": "[768]f32"
        }
    }
}